---
import { env } from "../env";
import LandingNavbar from "./islands/LandingNavbar";

interface Props {
	pace?: "landing" | "page";
}

const { pace = "page" } = Astro.props;
---

<LandingNavbar client:load appUrl={env.PUBLIC_CLIENT_URL} pace={pace} />
