import MockPanel from "../../kit/MockPanel";
import EncryptedDocHeader from "../../patterns/EncryptedDocHeader";

export default function SendStepMock() {
	return (
		<MockPanel variant="compact">
			<EncryptedDocHeader />
		</MockPanel>
	);
}
