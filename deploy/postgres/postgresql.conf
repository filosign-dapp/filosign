# Local dev-full Postgres (compose.dev-full.yml)
# Full pgBackRest backups work over the network; continuous WAL archive (PITR) uses
# archive_mode on staging/VPS - see project/ops/postgres-ops.md
listen_addresses = '*'
