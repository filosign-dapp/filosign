# Copy to `packages/emails/.env.local` or set ASTRO_URL in `apps/server/.env.local`
# Used by packages/emails/src/astro-assets.ts (static logo/icons in HTML email)

ASTRO_URL=http://localhost:3002 # Marketing site origin for email image URLs (match server ASTRO_URL)
