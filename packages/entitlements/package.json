{
	"name": "@filosign/entitlements",
	"version": "0.1.0",
	"exports": {
		".": {
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"check-types": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.tests.json",
		"test": "bun test tests/"
	},
	"dependencies": {
		"zod": "catalog:"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"typescript": "catalog:"
	}
}
