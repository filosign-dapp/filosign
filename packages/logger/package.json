{
	"name": "@filosign/logger",
	"version": "0.1.0",
	"exports": {
		".": {
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"check-types": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.tests.json",
		"test": "bun test tests/"
	},
	"dependencies": {
		"pino-pretty": "13.1.3",
		"pino": "10.3.1",
		"posthog-node": "5.1.1"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"@types/node": "catalog:",
		"typescript": "catalog:"
	}
}
